# Dependencias de desarrollo para testing y linting
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
mypy>=1.6.0
ruff>=0.1.0
httpx>=0.25.0

# Dependencias principales (instalar con pip install -e .)
# Ver pyproject.toml para las dependencias de runtime
